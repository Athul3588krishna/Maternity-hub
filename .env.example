# Server Configuration
PORT=5000
NODE_ENV=production

# MongoDB Connection String (MongoDB Atlas or local MongoDB)
MONGO_URI=mongodb+srv://<username>:<password>@cluster0.mongodb.net/maternity-hub?retryWrites=true&w=majority

# JWT Authentication Secret Key
JWT_SECRET=supersecretjwtkey_maternityhub

# Frontend API URL (leave blank for relative /api in production)
VITE_API_URL=http://localhost:5000/api
